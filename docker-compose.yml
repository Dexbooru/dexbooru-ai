services:
  qdrant:
    container_name: qdrant-vectordb
    image: qdrant/qdrant:latest
    ports:
      - 6333:6333
    environment:
      - QDRANT_STORAGE_PATH=/data
    volumes:
      - qdrant_data:/data

volumes:
  qdrant_data: